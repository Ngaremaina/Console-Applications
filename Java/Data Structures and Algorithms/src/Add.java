import java.util.Scanner;

public class Add {
    public static void main(String[] args) {
        String integers;
        Scanner scanner = new Scanner(System.in);

        System.out.println("Enter the values");
        integers=scanner.nextLine();

        add(integers)
        /*double[] numbers=new double[integers.length()];

        for (double i=0; i<numbers.length;i++){
            if (numbers[i]==" "){
                int index=numb
            }
            numbers[i]=integers.
        }

        char[] ch = new char[integers.length()];





        char [] item=integers.toCharArray();
        int
        int[] item=new int[integers.length];*/
        System.out.println(" "+ integers);



    }
}
